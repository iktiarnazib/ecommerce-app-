import 'package:flutter/material.dart';

class TColors {
  TColors._();

  //0XFF is to convert it to hexa decimal, the 4B68FF is the actual color
  static const Color primaryColor = Color(0xFF4B68FF);
}
